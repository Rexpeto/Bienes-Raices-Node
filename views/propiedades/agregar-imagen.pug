extends ../layout/admin

block styles
    link(rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css")

block contenido 
    .py-5 
        h2.text-center.text-2xl.font-extrabold=pagina
    
    if errores
        div(class="max-w-md mx-auto my-10")
            each error in errores
                p.bg-red-600.text-white.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg
    
    div(class="bg-white shadow px-4 py-8 rounded mx-auto max-w-4xl my-5 md:px-10")
        form(method='POST').space-y-8
            input(type="hidden", name="_csrf" value=csrfToken)

            input(type="submit", value="Públicar Propiedad" class="w-full py-2 bg-indigo-600 hover:bg-indigo-700 rounded cursor-pointer text-center text-white uppercase font-bold ")
            
block script 
    