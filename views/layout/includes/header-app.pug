header.bg-indigo-600.p-4
            .container.mx-auto.flex.justify-between.items-center
                a(href="/") 
                    h1.text-2xl.text-white.font-extrabold.text-center Bienes
                        span.font-normal Raices
                
                nav(class="my-2 text-sm md:flex md:items-center md:gap-4 text-white hidden md:initial")
                    a(href="/auth/login") Iniciar sesión
                    a(href="/auth/register") Crear cuenta
if categorias 
    div(class="bg-indigo-700 py-3 hidden md:block")
        div(class="container mx-auto flex justify-between text-sm items-center")
            nav(class="flex gap-4")
                each categoria in categorias 
                    a(href=`/categorias/${categoria.id}` class="text-sm font-bold uppercase text-white")= categoria.nombre
            
            form(action="/buscador" method="POST" class="flex gap-3 items-center")
                input(type="hidden" value=csrfToken name="_csrf")
                label(for="termino" class="text-sm font-bold text-white") Busqueda
                input(type="text" id="termino" name="termino" placeholder="Buscar propiedades" class="p-2 rounded-lg shadow") 
                input(type="submit" class="bg-indigo-800 rounded-lg text-white uppercase font-bold text-sm p-2 hover:bg-indigo-400 cursor-pointer" value="buscar")
            